@model Nop.Admin.Models.Events.EventModel
@{
    //page title
    ViewBag.Title = T("Admin.Events.Events.AddNew").Text;
    //active menu item (system name)
    Html.SetActiveMenuItemSystemName("Events");
}

@using (Html.BeginForm(null, null, FormMethod.Post, new { id = "event-form" }))
{
    @Html.AntiForgeryToken()
    <div class="content-header clearfix">
        <h1 class="pull-left">
            @T("Admin.Events.Events.AddNew")
            <small><i class="fa fa-arrow-circle-left"></i>@Html.ActionLink(T("Admin.Events.Events.BackToList").Text, "List")</small>
        </h1>
        <div class="pull-right">
            <button type="submit" name="save" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.Save")
            </button>
            <button type="submit" name="save-continue" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.SaveContinue")
            </button>
        </div>
    </div>
    @Html.Partial("_CreateOrUpdate", Model)
}
